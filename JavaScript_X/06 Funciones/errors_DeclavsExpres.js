/*
function sumar(a,b) {
    return a + b;
} 
Declarar una funcion o declaracion porque al principio escribimos la palabra reservada function

let sumar2 = function(a,b) {
    return a + b;
}

expresar una funcion o expresion en este caso asignamos una variable a una funcion puesto que la 
function no tiene nombre 

la diferencia entre las es que cuando tu haces una declaracion la puede declarar en cualquier
parte de tu codigo mientras que una expresion no por la variable que tiene como asignacion
*/
