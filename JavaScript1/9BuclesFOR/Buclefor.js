'use strict'
// BUCLE FOR
// Bucle es una estructura de control que se repite varias veces

var numero = 100;


for(var i = 0; i <= numero; i++){ 
    // la variable i es = a 0 y a la vez menor que variable numero 
    //but el valor de la variable i se incrementara paulatinamente. 
    console.log("Vamos por el numero: "+i);

    //debugger; 
    // es una instrucion de google para ver que sucede en bucle for
}

