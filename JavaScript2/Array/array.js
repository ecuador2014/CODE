/* Array o arreglos es una collecion de datos agrupados por
en una misma estructura*/

let arreglo = [89, 90, 91, 10];
for (var i = 0; i < arreglo.length; i++) {
  console.log(arreglo[i]);
}

/*ojo se puede guardar el recorrido del arreglo en un avariable
let elemento = arreglo[i];
*/
